@model IEnumerable<WebApplication1.Models.Item>

@{
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemName)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @*@Html.CheckBoxFor(m => m.item.Selected)*@
                @Html.DisplayFor(modelItem => item.ItemName)
            </td>
            <td>
                //hello vbnbvnbv  gfhfghfghfghffg

                @Html.ActionLink("Edit", "Edit", new { id=item.ItemCode }) |
                @Html.ActionLink("Details", "Details", new { id=item.ItemCode }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ItemCode })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
